export type Interface = Record<string, HTMLElement>;
export type UIConfig = {
    type: string;
    id?: string;
    class?: string;
    text?: string;
    placeholder?: string;
    tooltip?: string;
    html?: string;
    append?: string;
};